import { PublicKey } from "@solana/web3.js";

export const DEFAULT_SOL_ADDRESS: PublicKey = new PublicKey(
  "YourPublicKey", //Replace it with your Solana wallet address
);

export const DEFAULT_SOL_AMOUNT: number = 1.0;
